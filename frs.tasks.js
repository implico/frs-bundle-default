//custom tasks definitions
module.exports = function(appData) {
  //remove views task
  // appData.app.taskReg.removeTask('views');

  //create a task
  /*appData.taskReg['copy'] = {
    fn() {
      // task body...
      // remember to return a promise
      return Promise.resolve();
    }
  }
  //adds the task as a dependency: build task, before images dependency
  appData.app.taskReg.addDep('copy', 'build', 'images', true);*/

}